export class Ingredient {

  // convienient shortcut instead of creating class variables,
  // to pass them to constructor
  // and assign to this.<name> = <name>
  constructor(public name: string, public amount: number) {

  }
}
